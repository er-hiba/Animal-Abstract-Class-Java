// Define a Cat class extending the Animal class
class Cat extends Animal {
    // Constructor
    public Cat(String name, int age) {
        super(name, age);
    }

    // Implementing the sound method for a Cat
    public String sound() {
        return "Meowing";
    }
}
